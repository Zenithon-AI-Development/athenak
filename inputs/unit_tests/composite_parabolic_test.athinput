# AthenaK input file for the composite parabolic operator unit test
# (unit_tests/composite_parabolic_test).
#
# A 1D static, uniform-density medium carrying the global discrete cosine diffusion
# eigenmode, decomposed into FOUR MeshBlocks along x1 (mesh nx1=64, meshblock nx1=16).  The
# test builds TWO isotropic ConductionOperators with different conductivities over the same
# conserved energy field, wraps them in a parabolic::CompositeParabolicOperator (#114), and
# checks: the composite action equals the SUM of the two analytic eigenvalues (accumulate,
# not overwrite); ExplicitStableDt returns the minimum across sub-operators; the summed
# RKL2 super-step reproduces exp((lambda1+lambda2) t); and the global min-dt MPI all-reduce
# returns the same cross-rank minimum everywhere.  The conductivities are passed to the
# operators directly in code, so no <hydro> conductivity is set here.
#
# All checks run in ProblemGenerator::UserProblem; nlim = tlim = 0 (no time integration).
# x1 boundaries are outflow (NON-periodic) so the global domain ends are insulated while the
# three internal block faces exchange neighbor data (cross-rank under MPI).

<comment>
problem   = composite parabolic operator (summed action, min-dt, global reduce) unit test

<job>
basename  = composite_parabolic_test   # problem ID: basename of output filenames

<mesh>
nghost    = 2          # Number of ghost cells
nx1       = 64         # Number of zones in X1-direction (global)
x1min     = 0.0        # minimum value of X1
x1max     = 1.0        # maximum value of X1
ix1_bc    = outflow    # inner-X1 boundary flag (non-periodic => insulated domain end)
ox1_bc    = outflow    # outer-X1 boundary flag (non-periodic => insulated domain end)

nx2       = 1          # Number of zones in X2-direction
x2min     = -0.5       # minimum value of X2
x2max     = 0.5        # maximum value of X2
ix2_bc    = periodic   # inner-X2 boundary flag
ox2_bc    = periodic   # outer-X2 boundary flag

nx3       = 1          # Number of zones in X3-direction
x3min     = -0.5       # minimum value of X3
x3max     = 0.5        # maximum value of X3
ix3_bc    = periodic   # inner-X3 boundary flag
ox3_bc    = periodic   # outer-X3 boundary flag

<meshblock>
nx1       = 16         # 64/16 = 4 MeshBlocks along X1 (the multi-block decomposition)
nx2       = 1          # Number of cells in each MeshBlock, X2-dir
nx3       = 1          # Number of cells in each MeshBlock, X3-dir

<time>
evolution            = dynamic   # dynamic/kinematic/static
integrator           = rk2       # hyperbolic time integration algorithm
parabolic_integrator = sts       # operator-split parabolic backend (RKL2 STS)
cfl_number           = 0.3       # The Courant, Friedrichs, & Lewy (CFL) Number
nlim                 = 0         # cycle limit (0: do no integration, just run UserProblem)
tlim                 = 0         # time limit
ndiag                = 1         # cycles between diagnostic output

<hydro>
eos          = ideal   # EOS type
reconstruct  = plm     # spatial reconstruction method
rsolver      = llf     # Riemann-solver to be used
gamma        = 1.66666666666666667   # gamma = C_p/C_v

<problem>
pgen_name = composite_parabolic_test  # (ignored: built with -D PROBLEM=unit_tests/...)
