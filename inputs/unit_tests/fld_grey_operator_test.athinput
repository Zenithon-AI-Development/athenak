# AthenaK input file for the grey FLD operator unit test
# (unit_tests/fld_grey_operator_test).
#
# Minimal harness driver: all checks run in ProblemGenerator::UserProblem and the program
# exits before any time integration (nlim = tlim = 0).  A single physics block is required
# by MeshBlockPack::AddPhysics; we use a tiny <z4c> setup (matching the other unit tests)
# because the FLDGreyOperator is built and exercised entirely inside UserProblem on its own
# radiation-energy array and needs no matter initial conditions or field EOS.
#
# The test pins the Larsen flux limiter limits and verifies the operator action reproduces
# the discrete diffusion eigenvalue in the optically-thick limit (D = c/(3 chi)).

<comment>
problem   = grey FLD operator (Larsen limiter) unit test

<job>
basename  = fld_grey_operator_test   # problem ID: basename of output filenames

<mesh>
nghost    = 2          # Number of ghost cells
nx1       = 64         # Number of zones in X1-direction
x1min     = 0.0        # minimum value of X1
x1max     = 1.0        # maximum value of X1
ix1_bc    = outflow    # inner-X1 boundary flag
ox1_bc    = outflow    # outer-X1 boundary flag

nx2       = 1          # Number of zones in X2-direction
x2min     = -0.5       # minimum value of X2
x2max     = 0.5        # maximum value of X2
ix2_bc    = periodic   # inner-X2 boundary flag
ox2_bc    = periodic   # outer-X2 boundary flag

nx3       = 1          # Number of zones in X3-direction
x3min     = -0.5       # minimum value of X3
x3max     = 0.5        # maximum value of X3
ix3_bc    = periodic   # inner-X3 boundary flag
ox3_bc    = periodic   # outer-X3 boundary flag

<meshblock>
nx1       = 64         # Number of cells in each MeshBlock, X1-dir
nx2       = 1          # Number of cells in each MeshBlock, X2-dir
nx3       = 1          # Number of cells in each MeshBlock, X3-dir

<time>
evolution            = dynamic   # dynamic/kinematic/static
integrator           = rk2       # hyperbolic time integration algorithm
parabolic_integrator = sts       # operator-split parabolic backend (RKL2 STS)
cfl_number           = 0.3       # The Courant, Friedrichs, & Lewy (CFL) Number
nlim                 = 0         # cycle limit (0: do no integration, just run UserProblem)
tlim                 = 0         # time limit
ndiag                = 1         # cycles between diagnostic output

<problem>
c_light   = 1.0        # code-unit speed of light
chi       = 1.0e6      # extinction coefficient (huge => optically-thick diffusion limit)
n_larsen  = 2.0        # Larsen flux-limiter exponent
# ----- battery (E) free-streaming super-step positivity (issue #194) knobs -----
chi_thin  = 8.0        # transition-regime extinction coeff (R~4-8: RKL2-unstable band)
# (E2) monotone-front streaming-runaway knobs (#194): a SUSTAINED steep front in the
# RKL2-unstable band reproduces the coupled-run NaN on the unstabilized flux (RED),
# and the Lax-Friedrichs streaming term keeps erad_max bounded (GREEN).
chi_front     = 8.0    # front opacity (transition/streaming band, R~4-8)
esrc_front    = 1.0    # hot inner-x1 Dirichlet wall sustaining the front
e_hi          = 1.0    # bright (inner) side of the front
e_lo          = 1.0e-6 # cold (outer) side, re-imposed each step
dt_fac_front  = 5000.0 # super-step / explicit-dt ratio
nstep_front   = 300.0  # sustained-front super-steps
esrc_thin = -1.0       # insulated box (esrc<0): let the seeded checkerboard evolve freely
e_bg      = 1.0        # background radiation energy (O(1) so R sits in transition band)
seed_amp  = 0.5        # checkerboard seed amplitude (the most-amplified mode)
dt_fac    = 2.0e3      # super-step over explicit limit (large ratio => RKL2 over-steps)
nstep     = 1.5e2      # number of RKL2 super-steps in the sustained drive
fld_efloor = 1.0e-10   # erad positivity floor (#194): post-super-step projection level

<z4c>
