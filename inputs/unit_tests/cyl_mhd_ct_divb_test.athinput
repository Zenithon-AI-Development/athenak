# AthenaK input file for the cylindrical CT divergence-preservation unit test
# (unit_tests/cyl_mhd_ct_divb_test).
#
# Minimal harness driver: all checks run in ProblemGenerator::UserProblem (which builds a
# small cylindrical staggered field on local arrays) and the program exits before any time
# integration (nlim = tlim = 0).  A single physics block is required by
# MeshBlockPack::AddPhysics; we use a tiny <z4c> setup (matching the other unit tests)
# because it needs no matter initial conditions, no EOS, and no external data tables -- the
# test allocates and exercises its own cylindrical grid internally, independent of the mesh.

<comment>
problem   = cylindrical constrained-transport div(B)=0 unit test

<job>
basename  = cyl_mhd_ct_divb_test   # problem ID: basename of output filenames

<mesh>
nghost    = 2          # Number of ghost cells
nx1       = 8          # Number of zones in X1-direction
x1min     = -1.0       # minimum value of X1
x1max     = 1.0        # maximum value of X1
ix1_bc    = outflow    # inner-X1 boundary flag
ox1_bc    = outflow    # outer-X1 boundary flag

nx2       = 8          # Number of zones in X2-direction
x2min     = -1.0       # minimum value of X2
x2max     = 1.0        # maximum value of X2
ix2_bc    = outflow    # inner-X2 boundary flag
ox2_bc    = outflow    # outer-X2 boundary flag

nx3       = 8          # Number of zones in X3-direction
x3min     = -1.0       # minimum value of X3
x3max     = 1.0        # maximum value of X3
ix3_bc    = outflow    # inner-X3 boundary flag
ox3_bc    = outflow    # outer-X3 boundary flag

<meshblock>
nx1       = 8          # Number of cells in each MeshBlock, X1-dir
nx2       = 8          # Number of cells in each MeshBlock, X2-dir
nx3       = 8          # Number of cells in each MeshBlock, X3-dir

<time>
evolution  = dynamic   # dynamic/kinematic/static
integrator = rk3       # time integration algorithm
cfl_number = 0.1       # The Courant, Friedrichs, & Lewy (CFL) Number
nlim       = 0         # cycle limit (0: do no integration, just run UserProblem)
tlim       = 0         # time limit
ndiag      = 1         # cycles between diagnostic output

<z4c>
