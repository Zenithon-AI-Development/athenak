[flake8]
max-line-length = 90
# digitization_review/ holds committed, reproducible figure-extraction provenance
# scripts (WebPlotDigitizer post-processing, ADR-0008) -- not part of the test suite or
# shipped source, so they are excluded from the suite Python lint (like kokkos).
exclude = kokkos, cpplint.py, digitization_review